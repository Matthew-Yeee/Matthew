# Matthew 的个人复盘仓库

这个仓库用于记录 Matthew 的每周健康与生活复盘。Claude Code 在这里运行时，应遵循以下约定。

## 数据来源：Garmin MCP

本仓库通过 `.mcp.json` 配置了 `garmin`（[garmin-mcp-cn](https://github.com/BorisBW/garmin-mcp-cn)）MCP 服务器，
用于读取 Garmin 中国区（connect.garmin.cn）账号的数据（`GARMIN_IS_CN=true`）。

- 登录凭据通过环境变量 `GARMIN_EMAIL` / `GARMIN_PASSWORD` 注入，**不要**把它们写进本仓库的任何文件或对话记录。
  如果这两个变量还没有配置，先停下来提醒用户在当前环境的设置里添加，而不是继续尝试读取数据。
- 可用的数据类别包括：步数、心率、睡眠、压力（stress）、身体电量（Body Battery）、训练状态/训练负荷、
  运动记录（activities）、体重/体成分等。复盘时按需调用，不必每次都取全部数据。

## 每周复盘（每周日）

每周日运行一次复盘，流程：

1. 读取本文件（CLAUDE.md）了解上下文和历史约定。
2. 通过 `garmin` MCP 拉取过去 7 天（上周一到本周日）的数据：步数、睡眠、静息心率、压力、Body Battery、
   训练状态、主要运动记录；如果 `reviews/` 下有上一周的复盘，做简单的环比对比。
3. 在 `reviews/` 目录下新建一篇复盘，文件名为 `YYYY-MM-DD.md`（YYYY-MM-DD 为本周日日期），内容包含：
   - 本周关键数据摘要（步数、睡眠时长与质量、静息心率、压力、Body Battery 趋势）
   - 与上一周的对比（如有）
   - 值得关注的异常点（例如睡眠明显变差、压力持续偏高、训练负荷骤增等）
   - 简短的下周建议（1-3 条，具体可执行）
4. 提交并推送这次的复盘文件。
5. 如果 Garmin 登录失败（例如触发了验证码/二次验证，这在无人值守的自动化登录中比较常见），
   不要反复重试，如实记录失败原因，并在能读取到的历史数据基础上尽量完成复盘，或明确说明本周数据缺失。

复盘的语气：简洁、诚实，基于数据说话，不夸大也不说教。
